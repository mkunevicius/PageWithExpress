extends ./layout.jade
block content
    section.post
      .container
        .row
          .col-lg-8.col-lg-offset-2.text-center
            h2.section-heading #{inject.title}
            
            hr.light
            
            p.text-faded
              | #{inject.description}
            .article
              | #{inject.description}
